<Page x:Class="JoyReactor.Views.GalleryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:JoyReactor.Views"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:v="using:JoyReactor.Views"
      DataContext="{Binding Source={StaticResource Locator}, Path=Gallery}"
      mc:Ignorable="d">

    <Page.TopAppBar>
        <AppBar>
            <AppBarButton Icon="Back"
                          Label="Post"
                          Click="AppBarButton_Click" />
        </AppBar>
    </Page.TopAppBar>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <GridView ItemsSource="{Binding Images}">
            <GridView.ItemTemplate>
                <DataTemplate>
                    <v:ThumbnailImage ThumbnailSize="150"
                                      Width="150"
                                      Height="150"
                                      Source="{Binding}" />
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Page>